using UnityEngine;

public class MouseDisableStart : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Hides the cursor
        Cursor.visible = false;
    }
}
