using Unity.Cinemachine;
using UnityEditor;
using UnityEngine;

//[CustomEditor(typeof(TetherStripeParameters))]
public class EditorTetherStripes : Editor
{
    //private float stripeSize = 0.0f;
    //private float stripeAmount = 60.0f;
    //private float stripeScrollSpeed = -1.0f;
    //public override void OnInspectorGUI()
    //{
    //    TetherStripeParameters selected = target as TetherStripeParameters;

    //    stripeSize = EditorGUILayout.Slider("Stripe Size", stripeSize, -1f, 1f);
    //    stripeAmount = EditorGUILayout.Slider("Stripe Amount", stripeAmount, 1f, 50f);
    //    stripeScrollSpeed = EditorGUILayout.Slider("Stripe Scroll Speed", stripeScrollSpeed, -5f, 5f);

    //    if(selected != null )
    //    {

    //        selected.SetStripeThickness(stripeSize);
    //        selected.SetStripeCount(stripeAmount);
    //        selected.SetStripeScrollSpeed(stripeScrollSpeed);

    //        // Force scene to refresh
    //        SceneView.RepaintAll();
    //    }
    //}
}
